import './styles.css'

function Header() {
    return (
        <header className="fs-header">
            <nav>
                <a className="fs-logo" href="/" translate="no">Movie App</a>
            </nav>
        </header>
    )
}

export default Header